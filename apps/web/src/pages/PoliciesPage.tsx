import { Download, RefreshCw, Copy, Check, FileSpreadsheet } from "lucide-react";
import { useEffect, useState } from "react";
import { api, download } from "../lib/api";
import type { Policy } from "../lib/types";
import { dateTime, money } from "../lib/utils";
import { Badge, Button, Card, CardContent, Input, Select } from "../components/ui";

const statusStyle:Record<string,string>={ISSUED:"bg-emerald-100 text-emerald-700",FAILED:"bg-red-100 text-red-700",PROCESSING:"bg-blue-100 text-blue-700",QUEUED:"bg-amber-100 text-amber-700"};
const statusText:Record<string,string>={ISSUED:"Đã phát hành",FAILED:"Thất bại",PROCESSING:"Đang lấy link GCN...",QUEUED:"Đang xếp hàng..."};
export function PoliciesPage(){const[items,setItems]=useState<Policy[]>([]),[q,setQ]=useState(""),[status,setStatus]=useState(""),[loading,setLoading]=useState(true),[error,setError]=useState(""),[copiedId,setCopiedId]=useState<string|null>(null),[selectedMonth,setSelectedMonth]=useState(""),[selectedYear,setSelectedYear]=useState("");const getRecentMonths=()=>{const list=[];const now=new Date();for(let i=0;i<12;i++){const d=new Date(now.getFullYear(),now.getMonth()-i,1);const m=String(d.getMonth()+1).padStart(2,"0");const y=d.getFullYear();list.push({label:`Tháng ${m}/${y}`,month:m,year:y})}return list};const monthsList=getRecentMonths();const load=()=>{setLoading(true);let url=`/policies?q=${encodeURIComponent(q)}&status=${status}`;if(selectedMonth&&selectedYear){url+=`&month=${selectedMonth}&year=${selectedYear}`}api<{items:Policy[]}>(url).then(r=>setItems(r.items)).catch(e=>setError(e.message)).finally(()=>setLoading(false))};const copyLink=(id:string,url:string)=>{navigator.clipboard.writeText(url).then(()=>{setCopiedId(id);setTimeout(()=>setCopiedId(null),1500)})};const cleanPlateForFilename=(plate:string)=>{return plate.toUpperCase().replace(/[^A-Z0-9\s]/g,"").replace(/\s+/g," ").trim()};useEffect(load,[status,selectedMonth,selectedYear]);useEffect(()=>{const hasPending=items.some(p=>p.status==="QUEUED"||p.status==="PROCESSING");if(!hasPending)return;const timer=setInterval(()=>{let url=`/policies?q=${encodeURIComponent(q)}&status=${status}`;if(selectedMonth&&selectedYear){url+=`&month=${selectedMonth}&year=${selectedYear}`}api<{items:Policy[]}>(url).then(r=>setItems(r.items)).catch(e=>console.error("Polling error:",e))},4000);return ()=>clearInterval(timer)},[items,q,status,selectedMonth,selectedYear]);const handleMonthChange=(e:React.ChangeEvent<HTMLSelectElement>)=>{const val=e.target.value;if(!val){setSelectedMonth("");setSelectedYear("")}else{const[m,y]=val.split("-");setSelectedMonth(m);setSelectedYear(y)}};const exportUrl=`/policies/export?month=${selectedMonth}&year=${selectedYear}`;return <div className="space-y-6"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><h2 className="text-2xl font-bold">Danh sách đơn</h2><p className="text-muted-foreground">GCN, trạng thái và file PDF</p></div><div className="grid grid-cols-2 gap-2 sm:flex sm:items-center"><Input placeholder="Biển số, họ tên, GCN…" value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==="Enter"&&load()} className="col-span-2 sm:w-60"/><Select value={status} onChange={e=>setStatus(e.target.value)}><option value="">Mọi trạng thái</option><option value="QUEUED">Chờ xử lý</option><option value="PROCESSING">Đang xử lý</option><option value="ISSUED">Đã phát hành</option><option value="FAILED">Thất bại</option></Select><Select value={selectedMonth?`${selectedMonth}-${selectedYear}`:""} onChange={handleMonthChange}><option value="">Tất cả thời gian</option>{monthsList.map(item=><option key={`${item.month}-${item.year}`} value={`${item.month}-${item.year}`}>{item.label}</option>)}</Select><Button variant="outline" size="sm" onClick={()=>download(exportUrl,`danh_sach_phat_hanh_${selectedMonth||"all"}.xlsx`)} className="flex items-center gap-1.5 border-emerald-200 bg-emerald-50 text-emerald-700 hover:bg-emerald-100 h-10 px-3"><FileSpreadsheet size={15}/>Xuất Excel</Button><Button variant="outline" size="icon" onClick={load} className="h-10 w-10"><RefreshCw size={17}/></Button></div></div>{error&&<p className="rounded-lg bg-red-50 p-3 text-red-700">{error}</p>}<Card className="overflow-hidden"><CardContent className="overflow-x-auto p-0"><table className="w-full text-sm"><thead className="bg-stone-50 text-left"><tr><th className="p-3">Khách hàng</th><th className="p-3">Biển số</th><th className="p-3">GCN</th><th className="p-3">Phí</th><th className="p-3">Trạng thái</th><th className="p-3">Ngày tạo</th><th className="p-3"></th></tr></thead><tbody>{items.map(p=><tr key={p.id} className="border-t"><td className="p-3"><div className="font-medium">{p.customerName}</div>{p.user&&<div className="text-xs text-muted-foreground">{p.user.fullName}</div>}</td><td className="p-3 font-mono">{p.plateNumber}</td><td className="p-3 font-mono text-xs">{p.certificateNumber??"—"}</td><td className="p-3">{p.premium?money(p.premium):"—"}</td><td className="p-3"><Badge className={`flex items-center gap-1 w-fit ${statusStyle[p.status]}`}>{(p.status==="PROCESSING"||p.status==="QUEUED")&&<RefreshCw size={12} className="animate-spin"/>}{statusText[p.status]}</Badge>{p.error&&<div className="mt-1 max-w-xs truncate text-xs text-red-600" title={p.error}>{p.error}</div>}</td><td className="p-3 text-muted-foreground">{dateTime(p.createdAt)}</td><td className="p-3"><div className="flex items-center gap-2">{p.pdfPath&&<Button size="sm" variant="outline" onClick={()=>download(`/policies/${p.id}/pdf`,`${cleanPlateForFilename(p.plateNumber)}.pdf`)}><Download size={15}/>Tải xuống</Button>}{p.pdfUrl&&<Button size="sm" variant="outline" onClick={()=>copyLink(p.id,p.pdfUrl!)} className="flex items-center gap-1">{copiedId===p.id?(<><Check size={13} className="text-emerald-600"/>Đã chép</>):(<><Copy size={13}/>Copy link</>)}</Button>}</div></td></tr>)}{!loading&&!items.length&&<tr><td colSpan={7} className="p-12 text-center text-muted-foreground">Chưa có đơn nào</td></tr>}{loading&&<tr><td colSpan={7} className="p-12 text-center text-muted-foreground">Đang tải…</td></tr>}</tbody></table></CardContent></Card></div>}
