export type User = { id: string; username: string; fullName: string; phone?: string; role: "ADMIN" | "CTV" | "MANAGER"; status?: "ACTIVE" | "INACTIVE" | "LOCKED"; createdAt?: string; stats?: { count: number; premium: number } };
export type Policy = { id: string; customerName: string; plateNumber: string; certificateNumber?: string; serialNumber?: string; premium?: string; status: "QUEUED"|"PROCESSING"|"ISSUED"|"FAILED"; pdfPath?: string; pdfUrl?: string; createdAt: string; issuedAt?: string; error?: string; agent?: string; user?: Pick<User,"username"|"fullName"> };
export type Job = { id: string; type: "SINGLE_POLICY"|"EXCEL_UPLOAD"; status: "QUEUED"|"PROCESSING"|"COMPLETED"|"FAILED"; progress: number; error?: string; createdAt: string; batch?: { originalName: string; totalRows: number; issuedRows: number; failedRows: number } };
